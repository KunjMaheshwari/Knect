{
  "name": "stitch-playwright-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright test setup for the Stitch reminder dashboards",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
